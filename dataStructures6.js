// IMplement a function called countUniqueVallues
// which accepts a sorted array, and counts the
// unique values int eh array. There can be negative
// numbers in the array, but it will always be sorted.
// [1,1,1,1,1,1,2,2] returns 2
// [-2,-1,-1,0,1] returns 4

function countUniqueVallues(array){
    let uniqueValues = 0;
    let first = 0;
    let second = first + 1
    console.log(second);
    while(array[first] != array[second]){
        
    }
}

countUniqueVallues([0,2,4,5,6])